// include de cada uma das bibliotecas de cada método AQUII
#include "Solucao.h"

Solucao ILS(int maxIter, int maxIterIls) {
    Solucao bestOfAll;
    bestOfAll.cost = INFINITY;

    // aqui vai vir toda a lógica com cada uma das etapas
    for (int i = 0; i < maxIter; i++) {
        
    }

    return bestOfAll;
}